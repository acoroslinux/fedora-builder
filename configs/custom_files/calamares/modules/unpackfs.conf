# SPDX-FileCopyrightText: no
# SPDX-License-Identifier: CC0-1.0
---
# Fedora LiveOS uses an OverlayFS mount at /run/rootfsbase (the squashfs rootfs).
# Calamares unpacks from there into the target partition.
unpack:
    - source: "/run/rootfsbase"
      sourcefs: "squashfs"
      destination: ""
